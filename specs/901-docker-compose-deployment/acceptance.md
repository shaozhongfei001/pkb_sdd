# Acceptance: Docker Compose 部署

## A001 范围符合

实现内容必须只覆盖本 Spec 范围。

## A002 原始文件保护

执行本功能不得移动、删除、重命名、覆盖任何原始文件。

## A003 幂等性

重复执行同一输入，不得产生重复主记录或破坏已有状态。

## A004 异常可恢复

单个文件或单个任务失败时，应记录错误，不影响其他任务继续执行。

## A005 数据一致性

数据库记录、文件产物路径、状态流转必须一致。

## A006 测试通过

对应测试用例应通过，或记录人工验收结果。
